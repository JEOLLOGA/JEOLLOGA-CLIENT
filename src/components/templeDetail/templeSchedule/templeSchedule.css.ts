import { style } from '@vanilla-extract/css';

const templeScheduleContainer = style({
  display: 'flex',
  flexDirection: 'column',
  gap: '0.8rem',
  width: '33.5rem',
});

export default templeScheduleContainer;
